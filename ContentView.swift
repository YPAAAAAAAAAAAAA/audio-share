import SwiftUI

struct ContentView: View {
    var body: some View {
        MinimalStackedCards()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}